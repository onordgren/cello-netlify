extends layout

block content
  .container
    .product-information
      h1.product-title #{product.title}
      .product-description #{product.body}
  if product.images
    .product-images
      .container
        each image in product.images
          img.product-image(src=image.image)
